fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
asyncpg==0.30.0
# Floors (not exact pins) so recent langchain / langchain-google-genai can resolve their
# pydantic / pydantic-settings / python-dotenv requirements. Floors keep the versions the app
# was tested on.
python-dotenv>=1.0.1
pydantic>=2.10.4,<3
pydantic-settings>=2.14.2,<3
pydantic[email]
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20
httpx>=0.28.1
google-genai>=1.0.0
websockets>=14.1
gtts==2.5.4
pgvector==0.3.6
pypdf==5.1.0
python-docx==1.1.2
python-pptx==1.0.2
beautifulsoup4==4.12.3
langchain>=0.3.0
langchain-core>=0.3.0
langchain-google-genai>=4.0.0
kokoro>=0.9.2
soundfile>=0.12.1
matplotlib>=3.8.0
numpy>=1.26.0
apscheduler>=3.10.4
# Auth overhaul: OAuth (Authlib), session middleware (itsdangerous), and Casbin
# policy-based RBAC with an async SQLAlchemy policy adapter. (Email verification
# reuses platform_service.send_email — no extra SMTP dep needed.)
Authlib>=1.3.2
itsdangerous>=2.2.0
casbin>=1.36.0
casbin-async-sqlalchemy-adapter>=1.4.0
# Manim — pre-rendered maths/science ANIMATIONS (manim_service + manim_scenes). OPTIONAL: the app
# runs fully without it (show_animation reports disabled and the tutor falls back to a mermaid
# diagram). Needs the cairo/pango/ffmpeg system deps added in the Dockerfile.
#
# NOTE: the animation scenes label with manim's Text (Pango) rather than MathTex, so this does not
# pull the huge `texlive` SYSTEM package into the image. That is ONLY about the video renderer —
# it does NOT affect LaTeX in the app: equation puzzles render LaTeX with KaTeX in the browser
# (katex / react-katex); the AI authors valid LaTeX itself (validate → fix → retry, no server repair).
manim>=0.18.1
